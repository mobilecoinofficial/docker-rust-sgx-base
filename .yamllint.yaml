extends: default

rules:
  document-start: disable
  truthy:
    check-keys: false
  indentation:
    spaces: 2
    indent-sequences: false
  line-length: disable
  quoted-strings:
    quote-type: single
    required: only-when-needed
